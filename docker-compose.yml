services:
  jlpt-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: jlpt-bot:latest
    container_name: jlpt-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
